-- a projeção de uma query se dá pela lista de colunas que ela traz e é feita através do SELECT e do FROM

SELECT
    FIRST_NAME,
    LAST_NAME
FROM	
    HR.EMPLOYEES;

SELECT  
    FIRST_NAME, 
    LAST_NAME, 
    DEPARTMENT_ID, 
    HIRE_DATE
FROM    HR.EMPLOYEES;